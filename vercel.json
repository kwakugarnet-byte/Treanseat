{
  "buildCommand": "cd artifacts/bike-manager && pnpm install && pnpm run build",
  "outputDirectory": "artifacts/bike-manager/dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
